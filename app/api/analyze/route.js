import { NextResponse } from "next/server";
import { db } from "@/lib/firebase";
import { collection, getDocs, doc, updateDoc } from "firebase/firestore";

// 🧠 ฟังก์ชันคำนวณความคล้ายของคำ (Levenshtein)
function levenshtein(a, b) {
  const matrix = [];
  for (let i = 0; i <= b.length; i++) matrix[i] = [i];
  for (let j = 0; j <= a.length; j++) matrix[0][j] = j;

  for (let i = 1; i <= b.length; i++) {
    for (let j = 1; j <= a.length; j++) {
      matrix[i][j] =
        b[i - 1] === a[j - 1]
          ? matrix[i - 1][j - 1]
          : Math.min(
              matrix[i - 1][j - 1],
              matrix[i][j - 1],
              matrix[i - 1][j]
            ) + 1;
    }
  }
  return matrix[b.length][a.length];
}

export async function POST(req) {
  try {
    const { userId, baseCategories, transactions } = await req.json();

    // ✅ ตรวจสอบ userId
    if (!userId) {
      return NextResponse.json({ error: "❌ ต้องระบุ userId" }, { status: 400 });
    }

    // ✅ หมวดหมู่พื้นฐาน (ถ้าไม่ได้ส่งมา)
    const categoriesFromHome =
      baseCategories && baseCategories.length > 0
        ? baseCategories
        : [
            "อาหาร/เครื่องดื่ม",
            "ค่าที่อยู่อาศัย/เครื่องใช้",
            "ยานพาหนะ/การเดินทาง",
            "เสื้อผ้า/รองเท้า",
            "การสื่อสาร",
            "การศึกษา",
            "เวชภัณฑ์/ค่ารักษา",
            "บันเทิง",
            "อื่นๆ",
          ];

    // ✅ ดึงข้อมูลจาก Firestore แยกตามผู้ใช้
    let expenseTransactions = [];
    if (transactions && transactions.length > 0) {
      expenseTransactions = transactions;
    } else {
      const userTransRef = collection(db, `users/${userId}/transactions`);
      const snapshot = await getDocs(userTransRef);
      if (snapshot.empty) {
        return NextResponse.json({ message: "ยังไม่มีข้อมูลธุรกรรมของผู้ใช้นี้" });
      }
      const allTrans = snapshot.docs.map((doc) => doc.data());
      expenseTransactions = allTrans.filter((t) => t.type === "expense");
    }

    // ✅ ตรวจสอบว่ามีรายจ่ายไหม
    if (expenseTransactions.length === 0) {
      return NextResponse.json({ message: "ยังไม่มีข้อมูลรายจ่ายของผู้ใช้" });
    }

    // ✅ คำใบ้หมวดหมู่ที่ละเอียดและแม่นยำยิ่งขึ้น
    const categoryHints = {
      "ยานพาหนะ/การเดินทาง": [
        // น้ำมันรถ
        "น้ำมัน", "เติมน้ำมัน", "ปตท", "esso", "shell", "caltex", "bangchak", "gas", "fuel", "petrol", "diesel", "benzin", "gasoline",
        // ค่าโดยสาร
        "แท็กซี่", "รถเมล์", "รถแดง", "รถสองแถว", "BTS", "MRT", "skytrain", "แกร็บ", "grab", "bolt", "รถไฟ", "รถไฟฟ้า", "รถตู้", "มินิบัส", "taxi", "uber", "bus", "train", "van",
        // เครื่องบิน
        "เครื่องบิน", "สายการบิน", "ตั๋วเครื่องบิน", "ตั๋วบิน", "airasia", "nok air", "thai airways", "flight", "airline", "airplane",
        // จอดรถ
        "จอดรถ", "ค่าจอด", "ที่จอดรถ", "parking",
        // ซ่อมรถ
        "ซ่อมรถ", "เปลี่ยนยาง", "เช็คระยะ", "ล้างรถ", "car wash", "repair",
        // อื่นๆ
        "เดินทาง", "ค่าโดยสาร", "ค่ารถ", "transportation", "travel"
      ],
      "อาหาร/เครื่องดื่ม": [
        // เครื่องดื่ม (ตรวจก่อน "น้ำ")
        "กาแฟ", "คาเฟ่", "สตาร์บัคส์", "starbucks", "amazon", "cafe", "coffee", "espresso", "latte", "cappuccino", "americano",
        "ชา", "ชาเขียว", "ชาไทย", "tea", "milk tea", "green tea",
        "เครื่องดื่ม", "น้ำดื่ม", "น้ำอัดลม", "โค้ก", "coke", "pepsi", "sprite", "soft drink", "beverage", "juice", "smoothie",
        // อาหาร
        "ข้าว", "ข้าวผัด", "ข้าวราดแกง", "ข้าวมันไก่", "ก๋วยเตี๋ยว", "ผัดไทย", "ส้มตำ", "rice", "noodle",
        "อาหาร", "กินข้าว", "มื้อเช้า", "มื้อกลางวัน", "มื้อเย็น", "ของว่าง", "food", "meal", "breakfast", "lunch", "dinner", "snack",
        "ร้านอาหาร", "ภัตตาคาร", "โรงอาหาร", "ฟู้ดคอร์ท", "restaurant", "food court", "canteen", "buffet",
        // ฟาสต์ฟู้ด
        "พิซซ่า", "pizza", "burger", "เบอร์เกอร์", "แมคโดนัลด์", "mcdonald", "kfc", "เคเอฟซี", "subway", "domino",
        "แซนวิช", "แซนด์วิช", "เเวนวิท", "sandwich", "sub",
        // ของหวาน
        "ขนม", "เค้ก", "ไอศกรีม", "เบเกอรี่", "cake", "ice cream", "dessert", "bakery", "donut", "chocolate",
        // วัตถุดิบ
        "ผัก", "ผลไม้", "เนื้อ", "หมู", "ไก่", "ปลา", "ไข่", "นม", "ขนมปัง", "vegetable", "fruit", "meat", "pork", "chicken", "fish", "egg", "milk", "bread",
        // ตลาด/ซูเปอร์
        "ตลาด", "เซเว่น", "7-11", "big c", "lotus", "tops", "makro", "market", "supermarket", "convenience store",
        // อื่นๆ
        "น้ำ", "drink", "food", "eating"
      ],
      "ค่าที่อยู่อาศัย/เครื่องใช้": [
        // ค่าเช่า
        "ค่าเช่า", "เช่าบ้าน", "เช่าคอนโด", "เช่าห้อง", "ห้องเช่า", "rent", "rental",
        "บ้าน", "คอนโด", "ห้อง", "อพาร์ทเม้นท์", "apartment", "condo", "house", "room",
        // ค่าน้ำ ค่าไฟ
        "ค่าไฟ", "ค่าไฟฟ้า", "ไฟฟ้า", "electric", "electricity", "กฟน", "mea", "pea",
        "ค่าน้ำ", "ค่าน้ำประปา", "ประปา", "water", "water bill",
        // เครื่องใช้ไฟฟ้า
        "เครื่องใช้ไฟฟ้า", "แอร์", "เครื่องปรับอากาศ", "air conditioner", "ac",
        "ตู้เย็น", "refrigerator", "fridge",
        "ทีวี", "โทรทัศน์", "television", "tv",
        "พัดลม", "fan",
        "เครื่องซักผ้า", "washing machine",
        // เฟอร์นิเจอร์
        "เฟอร์นิเจอร์", "โซฟา", "เตียง", "ตู้", "โต๊ะ", "เก้าอี้", "furniture", "sofa", "bed", "table", "chair", "cabinet",
        // ของใช้ในบ้าน
        "ของใช้", "ของใช้ในบ้าน", "ผงซักฟอก", "น้ำยาล้างจาน", "ไม้กวาด", "household", "detergent", "soap"
      ],
      "เสื้อผ้า/รองเท้า": [
        "เสื้อ", "เสื้อผ้า", "เสื้อยืด", "เสื้อเชิ้ต", "shirt", "t-shirt", "blouse", "top", "clothes", "clothing",
        "กางเกง", "กางเกงยีนส์", "กางเกงขาสั้น", "pants", "jeans", "shorts", "trousers",
        "กระโปรง", "ชุด", "ชุดเดรส", "skirt", "dress",
        "รองเท้า", "รองเท้าผ้าใบ", "รองเท้าแตะ", "รองเท้าบูท", "shoes", "sneakers", "sandals", "boots", "footwear",
        "ถุงเท้า", "socks",
        "หมวก", "หมวกแก๊ป", "hat", "cap",
        "กระเป๋า", "กระเป๋าสตางค์", "กระเป๋าเป้", "bag", "backpack", "wallet", "purse",
        "เข็มขัด", "belt",
        "แว่นตา", "glasses", "sunglasses",
        "เครื่องประดับ", "jewelry",
        // แบรนด์
        "uniqlo", "h&m", "zara", "nike", "adidas", "converse", "vans", "crocs", "jaspal", "mc", "lyn"
      ],
      "การสื่อสาร": [
        "มือถือ", "โทรศัพท์", "ค่าโทร", "ค่าโทรศัพท์", "phone", "mobile", "telephone",
        "อินเทอร์เน็ต", "อินเตอร์เน็ต", "เน็ต", "ค่าเน็ต", "wifi", "internet", "broadband", "fiber",
        "ซิม", "ซิมการ์ด", "sim", "sim card",
        "เติมเงิน", "top up", "refill",
        "แพ็กเกจ", "package", "data",
        // ผู้ให้บริการ
        "true", "ais", "dtac", "ทรู", "เอไอเอส", "ดีแทค", "nt", "3bb", "tot"
      ],
      "การศึกษา": [
        "เรียน", "ค่าเรียน", "ค่าเล่าเรียน", "ค่าเทอม", "tuition", "school fee", "education",
        "หนังสือ", "หนังสือเรียน", "ตำรา", "book", "textbook",
        "ติว", "คอร์สเรียน", "คอร์ส", "อบรม", "สอน", "course", "tutorial", "training", "class", "lesson",
        "ปากกา", "ดินสอ", "สมุด", "ยางลบ", "ไม้บรรทัด", "pen", "pencil", "notebook", "eraser", "ruler",
        "เครื่องเขียน", "stationery",
        "โรงเรียน", "มหาวิทยาลัย", "วิทยาลัย", "school", "university", "college",
        "ค่าสอบ", "exam", "test"
      ],
      "เวชภัณฑ์/ค่ารักษา": [
        "ยา", "ยาแก้ปวด", "ยาแก้ไข้", "ยาแก้หวัด", "ยาแก้ท้อง", "medicine", "drug", "pill", "tablet",
        "วิตามิน", "อาหารเสริม", "vitamin", "supplement",
        "หมอ", "แพทย์", "นพ", "doctor", "physician",
        "โรงพยาบาล", "รพ", "hospital",
        "คลินิก", "clinic",
        "รักษา", "ตรวจ", "ตรวจรักษา", "ตรวจโรค", "treatment", "checkup", "check up",
        "พยาบาล", "nurse",
        "ทันตแพทย์", "หมอฟัน", "ฟัน", "dentist", "dental",
        "ร้านขายยา", "ร้านยา", "pharmacy", "drug store",
        "ประกัน", "ประกันสุขภาพ", "insurance", "health insurance"
      ],
      "บันเทิง": [
        "หนัง", "ดูหนัง", "โรงหนัง", "ซีเนพเลกซ์", "เมเจอร์", "movie", "cinema", "sf", "major", "cgv",
        "เกม", "เกมส์", "เล่นเกม", "game", "gaming", "steam", "playstation", "xbox", "nintendo", "garena",
        "คอนเสิร์ต", "งานเทศกาล", "concert", "festival", "event",
        "ปาร์ตี้", "งานเลี้ยง", "party", "celebration",
        "เที่ยว", "ท่องเที่ยว", "ทริป", "เที่ยวทะเล", "เที่ยวภูเขา", "travel", "trip", "tour", "vacation", "holiday",
        "ของขวัญ", "ของกำนัล", "gift", "present",
        "ผับ", "บาร์", "ไนท์คลับ", "pub", "bar", "club", "nightclub",
        "สวนสนุก", "สวนสัตว์", "พิพิธภัณฑ์", "amusement park", "zoo", "museum",
        "คาราโอเกะ", "karaoke",
        "นวด", "สปา", "massage", "spa",
        "ฟิตเนส", "ยิม", "gym", "fitness",
        // Streaming
        "netflix", "spotify", "youtube premium", "disney+", "hbo", "prime video", "apple music"
      ],
      "อื่นๆ": [
        "บริจาค", "ทำบุญ", "ใส่บาตร", "donate", "donation", "charity",
        "ของฝาก", "ของที่ระลึก", "souvenir",
        "งานพิธี", "งานบวช", "งานแต่ง", "งานศพ", "ceremony", "wedding", "funeral",
        "ซ่อมของ", "ซ่อม", "repair", "fix",
        "ทั่วไป", "อื่นๆ", "other", "misc", "miscellaneous"
      ]
    };

    // ✅ เตรียม object เก็บยอดรวมหมวดหมู่
    const categoryTotals = Object.fromEntries(categoriesFromHome.map((c) => [c, 0]));

    // 🧩 ฟังก์ชันจำแนกหมวด (ปรับให้เช็คคำที่ยาวกว่าก่อน)
    const classifyExpense = (title = "") => {
      const lower = title.toLowerCase().trim();
      let best = "อื่นๆ";
      let minDist = Infinity;

      // เรียงลำดับหมวดหมู่: ยานพาหนะก่อน (เพราะมี "น้ำมัน") แล้วค่อยหมวดอื่น
      const orderedHints = [
        ["ยานพาหนะ/การเดินทาง", categoryHints["ยานพาหนะ/การเดินทาง"]],
        ["อาหาร/เครื่องดื่ม", categoryHints["อาหาร/เครื่องดื่ม"]],
        ["ค่าที่อยู่อาศัย/เครื่องใช้", categoryHints["ค่าที่อยู่อาศัย/เครื่องใช้"]],
        ["เสื้อผ้า/รองเท้า", categoryHints["เสื้อผ้า/รองเท้า"]],
        ["การสื่อสาร", categoryHints["การสื่อสาร"]],
        ["การศึกษา", categoryHints["การศึกษา"]],
        ["เวชภัณฑ์/ค่ารักษา", categoryHints["เวชภัณฑ์/ค่ารักษา"]],
        ["บันเทิง", categoryHints["บันเทิง"]],
        ["อื่นๆ", categoryHints["อื่นๆ"]],
      ];

      // เช็คแบบ exact match ก่อน (เรียงจากคำยาวไปสั้น)
      for (const [cat, words] of orderedHints) {
        // เรียงคำจากยาวไปสั้น
        const sortedWords = [...words].sort((a, b) => b.length - a.length);
        for (const kw of sortedWords) {
          if (lower.includes(kw.toLowerCase())) {
            return cat;
          }
        }
      }

      // ถ้าไม่เจอ exact match ใช้ Levenshtein distance
      for (const [cat, words] of orderedHints) {
        for (const kw of words) {
          const dist = levenshtein(lower, kw.toLowerCase());
          if (dist < minDist && dist <= 3) {
            minDist = dist;
            best = cat;
          }
        }
      }
      
      return best;
    };

    // ✅ รวมยอดรายจ่ายตามหมวด และอัปเดตหมวดหมู่กลับไป Firestore
    const updatePromises = []; // เก็บ promises สำหรับอัปเดต
    const classifiedItems = []; // เก็บรายการที่จำแนกแล้ว
    
    for (const t of expenseTransactions) {
      const category = classifyExpense(t.title || t.name || "");
      if (categoryTotals[category] !== undefined) {
        categoryTotals[category] += Number(t.amount) || 0;
      }
      
      // เก็บรายการพร้อมหมวดหมู่
      classifiedItems.push({
        name: t.title || t.name || "",
        amount: Number(t.amount) || 0,
        category: category,
        date: t.date
      });
      
      // 🤖 อัปเดตหมวดหมู่กลับไป Firestore (ถ้ามี id)
      if (t.id && (!t.category || t.category === "อื่นๆ" || t.category === "")) {
        const docRef = doc(db, "transactions", t.id);
        updatePromises.push(
          updateDoc(docRef, { category }).catch(err => {
            console.warn(`ไม่สามารถอัปเดต ${t.id}:`, err.message);
          })
        );
      }
    }
    
    // รอให้อัปเดตเสร็จ
    if (updatePromises.length > 0) {
      await Promise.all(updatePromises);
      console.log(`✅ อัปเดตหมวดหมู่สำเร็จ ${updatePromises.length} รายการ`);
    }

    // ✅ แปลงเป็น array สำหรับส่งกลับ
    const categories = categoriesFromHome.map((name) => ({
      name,
      total: categoryTotals[name],
    }));

    // 🔍 วิเคราะห์เชิงลึก
    const totalExpense = categories.reduce((sum, c) => sum + c.total, 0);
    
    // หาหมวดที่ใช้เยอะสุด 3 อันดับแรก
    const topCategories = [...categories]
      .filter(c => c.total > 0)
      .sort((a, b) => b.total - a.total)
      .slice(0, 3);

    // คำนวณเปอร์เซ็นต์
    const categoriesWithPercent = categories.map(c => ({
      ...c,
      percent: totalExpense > 0 ? Math.round((c.total / totalExpense) * 100) : 0
    }));

    // 💡 สร้างคำแนะนำที่ละเอียดขึ้น
    let advice = "";
    
    if (totalExpense === 0) {
      advice = "ยังไม่มีข้อมูลรายจ่ายในเดือนนี้ เริ่มบันทึกรายจ่ายเพื่อให้ AI วิเคราะห์ให้คุณได้นะคะ 📊";
    } else if (topCategories.length > 0) {
      const top1 = topCategories[0];
      const top1Percent = Math.round((top1.total / totalExpense) * 100);
      
      // วิเคราะห์ตามหมวดหมู่
      if (top1.name === "อาหาร/เครื่องดื่ม" && top1Percent > 40) {
        advice = `🍜 คุณใช้จ่ายกับ "${top1.name}" ถึง ${top1Percent}% ของรายจ่ายทั้งหมด (${top1.total.toLocaleString()} บาท) ซึ่งค่อนข้างสูง แนะนำให้ลองทำอาหารเองมากขึ้น หรือหาโปรโมชั่นร้านอาหารเพื่อลดค่าใช้จ่ายในหมวดนี้`;
      } else if (top1.name === "บันเทิง" && top1Percent > 30) {
        advice = `🎉 คุณใช้จ่ายกับ "${top1.name}" ถึง ${top1Percent}% ของรายจ่ายทั้งหมด (${top1.total.toLocaleString()} บาท) ซึ่งค่อนข้างสูง ลองพิจารณาลดความบันเทิงบางส่วน หรือหากิจกรรมฟรีๆ ทดแทนเพื่อความสมดุลทางการเงิน`;
      } else if (top1.name === "ยานพาหนะ/การเดินทาง" && top1Percent > 25) {
        advice = `🚗 คุณใช้จ่ายกับ "${top1.name}" ถึง ${top1Percent}% ของรายจ่ายทั้งหมด (${top1.total.toLocaleString()} บาท) ลองพิจารณาใช้ระบบขนส่งสาธารณะ หรือแชร์รถกับเพื่อนร่วมงานเพื่อประหยัดค่าเดินทาง`;
      } else if (top1.name === "เสื้อผ้า/รองเท้า" && top1Percent > 20) {
        advice = `👗 คุณใช้จ่ายกับ "${top1.name}" ถึง ${top1Percent}% ของรายจ่ายทั้งหมด (${top1.total.toLocaleString()} บาท) ลองรอช่วงลดราคา หรือซื้อของที่จำเป็นจริงๆ เพื่อไม่ให้เกินงบ`;
      } else {
        // คำแนะนำทั่วไป
        const ranking = topCategories.map((c, i) => 
          `${i + 1}. ${c.name} (${Math.round((c.total / totalExpense) * 100)}% - ${c.total.toLocaleString()} บาท)`
        ).join(", ");
        
        advice = `📊 เดือนนี้คุณใช้จ่ายทั้งหมด ${totalExpense.toLocaleString()} บาท โดยหมวดที่ใช้มากที่สุดคือ: ${ranking}. `;
        
        if (topCategories.length >= 2 && topCategories[0].total > topCategories[1].total * 2) {
          advice += `สังเกตว่าคุณใช้จ่ายใน "${topCategories[0].name}" มากกว่าหมวดอื่นเกือบเท่าตัว ลองเช็คว่ามีรายการใดที่สามารถลดลงได้บ้าง`;
        } else {
          advice += `การกระจายรายจ่ายของคุณค่อนข้างสมดุล แต่อย่าลืมติดตามว่าอยู่ในงบที่กำหนดไว้นะคะ`;
        }
      }
    }

    // ✅ ส่งกลับข้อมูล
    return NextResponse.json({ 
      data: { 
        categories, 
        categoriesWithPercent,
        classifiedItems, // ส่งรายการที่จำแนกแล้ว
        advice,
        summary: {
          totalExpense,
          topCategory: topCategories[0] || null,
          categoryCount: categories.filter(c => c.total > 0).length,
          transactionCount: expenseTransactions.length
        }
      } 
    }, { status: 200 });
  } catch (error) {
    console.error("❌ Error:", error);
    return NextResponse.json(
      {
        error: "เกิดข้อผิดพลาดในการประมวลผลข้อมูล",
        details: error.message,
      },
      { status: 500 }
    );
  }
}
